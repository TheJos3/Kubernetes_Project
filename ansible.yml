- hosts: all
  connection: local
  become: true
  tasks:
    - name: create new deployment
      raw: kubectl apply -f /home/alumno/Deployment.yml
    - name: create new service
      raw: kubectl apply -f /home/alumno/Service.yml
