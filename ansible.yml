- hosts: all
  connection: local
  become: true
  tasks:
    - name: create new deployment
      shell: kubectl apply -f /home/alumno/Deployment.yml
    - name: create new service
      shell: kubectl apply -f /home/alumno/Service.yml
